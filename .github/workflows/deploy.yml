name: Deploy to Cloud
on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: echo "Тут могут быть тесты, например npm test"

    - name: Deploy to Cloud
      run: echo "Автоматический деплой будет выполнен через облачный сервис"
